<h1>Adnat</h1>
<p>Logged in as <%= @user.name %> <%= link_to 'Log out', login_index_path %> <br>

<h2><%= @organisation.name %></h2>
<%= link_to 'View Shifts', shifts_url(organisationId: @organisation.id) %> 
<%= link_to 'Edit', edit_organisation_url(@organisation) %>
<%= link_to 'Leave', leaveOrganisation_path(userId: @user.id) %>

