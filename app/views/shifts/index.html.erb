<h1> ADD TEMPLATE FOR HEADER </h1>
<p> Add template for Logged in as... </p>

<h2><%= @organisation.name %></h2>

<h3>Shifts</h3>
<table>
  <% @shifts.each do |shift| %>   
    <tr>
        <td> 
          <p> some test text yall </p>
        </td>
    </tr>
  <% end %>
</table>